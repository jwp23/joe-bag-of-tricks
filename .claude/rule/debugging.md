# Systematic Debugging
YOU MUST ALWAYS find the root cause — never patch a symptom or add a workaround.
FOR ANY DEBUGGING TASK (failing hook, broken script, skill not triggering), YOU MUST invoke
`/joe-bag-of-tricks:systematic-debugging` via the Skill tool before reading code or proposing fixes.
