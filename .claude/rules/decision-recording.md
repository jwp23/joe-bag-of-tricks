# Decision Recording Rules
Never make a significant technical or customization choice without recording it — especially
fork-divergence decisions (what to replace vs. patch, why an upstream skill was forked).
YOU MUST invoke `/joe-bag-of-tricks:record-decision` via the Skill tool to capture the choice
and its trade-offs. Routine per-file classifications also get a row in `docs/customizations.md`.
