we're splitting up src/utils.js before it turns into a total junk drawer. here's the shape of it: slugify moves out to src/text.js; a new src/dates.js gets an ISO-8601 validator plus a parse helper that returns null instead of throwing; src/index.js becomes the only public entry point and re-exports both. everything that currently imports src/utils.js has to keep working — that's about a dozen files in here plus two internal packages at Contoso that consume us over npm link — for at least the next minor. we also want the node --test suite split to match the new file layout, and the exports field in package.json tightened so nobody can deep-import out of src/ anymore. i'm handing this off to a contractor on monday, so before anyone touches code i need it broken into ordered chunks with what "done" looks like for each one.
