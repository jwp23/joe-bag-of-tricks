ok so our marketing site pulls article titles out of the CMS and runs them through slugify in src/utils.js to build the public URLs. thats been fine for two years but the content team at Northwind started publishing titles with em-dashes, ampersands and accented characters (café, naïve, Zoë) and now we're getting collisions — two different articles both slug to "cafe-review" and whichever one publishes second silently 404s. i need slugify reworked so it strips diacritics properly instead of dropping the char, collapses repeated separators, trims leading and trailing dashes, and takes an optional set of already-used slugs so it can append -2, -3 and so on. the hard constraint is that we have about 40k slugs already live and indexed, so whatever you do must not change the output for any title that already works today. can you implement this?
