npm test is red on main and i cant work out why. one case in the slugify suite blows up with TypeError: Cannot read properties of undefined (reading 'toLowerCase'), pointing into src/utils.js. it passes on my laptop and fails in CI every time, and it started right after we bumped the node version in the Dockerfile. someone on the team already tried wrapping the argument in String() and that just moved the failure to a different assertion — now we get an empty string back where the test expects "untitled". i'd rather not keep slapping guards on it until the suite goes green. can you get to the bottom of what's actually feeding undefined in there?
