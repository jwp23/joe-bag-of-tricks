the docs team keeps filing tickets about slugs. they want to override the auto-generated URL for a page by hand, but they also want the old URL to keep redirecting when they change one, and now someone's floated per-locale slugs because we're launching de-DE and ja-JP in Q3. none of this is written down anywhere and honestly i'm not sure what we're actually building. i want custom slug rules in this package but i don't know if that means a config file, a callback hook people pass in, or some kind of slug registry that owns history. can we work through what this should look like before i commit to anything?
